# This is a comprehensive .gitignore file for .NET solutions.
# It prevents temporary files, build artifacts, user-specific settings,
# and sensitive data from being committed to source control.

#-------------------------------------------------------------------
# SECTION 1: ESSENTIALS - VISUAL STUDIO & BUILD ARTIFACTS
# These are the absolute must-haves for any .NET project.
#-------------------------------------------------------------------

# User-specific files generated by Visual Studio.
# .vs folder contains user-specific settings like open files, window layout, and breakpoints.
.vs/
*.suo
*.user
*.sln.docstates

# Build results. These are generated every time you compile.
bin/
obj/

#-------------------------------------------------------------------
# SECTION 2: IDE & EDITOR-SPECIFIC FILES
# Covers common IDEs used for .NET development.
#-------------------------------------------------------------------

# JetBrains Rider
.idea/

# VS Code
.vscode/

#-------------------------------------------------------------------
# SECTION 3: SENSITIVE DATA & SECRETS
# Crucial for preventing secret keys, connection strings, etc., from being exposed.
#-------------------------------------------------------------------

# User secrets file. The UserSecretsId in the .csproj is SAFE to commit.
secrets.json

# Environment-specific settings files that often contain secrets.
# It's standard practice to commit appsettings.json (as a template) but
# ignore local development overrides.
appsettings.Development.json
appsettings.*.json

# Un-ignore the main settings file and production settings if they don't contain secrets.
# A "!" prefix negates a previous rule.
!appsettings.json
!appsettings.Production.json

# Environment variable files
.env

#-------------------------------------------------------------------
# SECTION 4: PACKAGE MANAGEMENT
# Ignores local package caches.
#-------------------------------------------------------------------

# NuGet packages folder (for older .NET Framework projects using packages.config)
packages/

# npm/yarn package folders (for web projects with front-end dependencies)
node_modules/
npm-debug.log*
yarn-error.log*

#-------------------------------------------------------------------
# SECTION 5: TESTING & CODE COVERAGE
# Ignores test results and reports which are generated locally or by build servers.
#-------------------------------------------------------------------

TestResults/
*.trx
*.coverlet.json
*.cobertura.xml

#-------------------------------------------------------------------
# SECTION 6: BUILD & PUBLISH ARTIFACTS
# Ignores files generated when you publish or package your application.
#-------------------------------------------------------------------

publish/
Release/
Releases/
*.nupkg
*.snupkg

#-------------------------------------------------------------------
# SECTION 7: MISCELLANEOUS & OS-SPECIFIC
# General clutter from different operating systems and tools.
#-------------------------------------------------------------------

# Log files
*.log
*.log.*

# Temporary files
*.tmp

# Windows thumbnail cache
Thumbs.db

# macOS custom folder attributes
.DS_Store

# ReSharper cache
_ReSharper.*
*.resharper